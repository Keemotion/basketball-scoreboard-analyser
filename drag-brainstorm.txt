drag-handler

separate selection and dragger

ctrl-drag = drag
shif-drag = area select
shift-click = select and keep previous
ctrl-click = select and replace previous


CanvasSelectHandler:
  shift-click
  shift-drag
  ctrl-click


